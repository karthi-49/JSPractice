let browser = "chrome"
let version = 1
let os = "MAC"

// AND = &&
// OR = ||

if (browser == "chrome" || os == "Windows" || version >=2 )
{
    console.log("Supported version");
    
}else
    {
        console.log("Not supported version");
        
    }